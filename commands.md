```bash
terraform init # download the specified version of the Kubernetes provider
terraform plan # to display a list of resources to be created, and highlight any possible unknown attributes at apply time.
terraform apply # start provisioning (deployment)
terraform destroy # destroys deployment
```